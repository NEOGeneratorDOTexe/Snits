0.<!--the <!DOCTYPE> is not a tag, but rather a declaration. It is NOT case sensitive.
  <!dOcTyPe html> is informing a webclient about what document type to expect. -->
1.rel && href attributes <!--the rel attribute defines the relationship between a linked resource and the current document. -->
  <!-- two types of href 1.[(absolute) URL] -> points to another web site (like href="http://www.example.com/theme.css"). 2.[(relative) URL] -> points to a file within a web site (folder/resources) (like href="/themes/theme.css") -->
2.
